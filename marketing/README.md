# Community seeding kit

Ready-to-paste drafts for launch day. Tone: honest, specific, zero trash-talk.
Read each community's self-promotion rules before posting — several Discord
servers require a wait period or a dedicated channel.

Post order: Discord servers → Reddit → X thread → dev.to article (links
resolve to the published version by then).

Before posting: replace every `YOUR_LINK` placeholder and confirm
`npm install junie` works.
